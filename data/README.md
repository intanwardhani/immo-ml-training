This directory contains two data folders.

1. The `raw` data folder contains raw real-estate data extracted from the webscraping project based on Immovlan website.
2. The `processed` data folder contains the cleaned data. The cleaning tool used for this can be found in `src/data/preprocess_data.py`.
